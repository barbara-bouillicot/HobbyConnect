<div class="container">
  <h1>All users</h1>

  <% @users.each do |user| %>

    <div class="user-card">
      <h2><%= link_to user.username, user_path(user) %></h2>
      <h3>Hobbies: X X X X X </h3>
    </div>

  <% end %>

</div>
